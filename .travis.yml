language: ruby
rvm:
- 2.3.0
before_install: gem update --remote bundler
install:
- bundle install --retry=3
script:
- bundle exec rspec
services: 
- postgresql
script:
- bundle exec rake db:test:prepare
- bundle exec rspec